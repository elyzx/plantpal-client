import React from 'react';

function Login() {
    return (
        <div>
            Hello I'm the login form
            {/* after login, render the dashboard */}
        </div>
    );
};

export default Login;